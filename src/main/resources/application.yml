spring:
  application:
    name: homa-planning-poker
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres
    url: jdbc:postgresql://localhost:5432/postgres

rest:
  client:
    default:
      timeout: 10000
